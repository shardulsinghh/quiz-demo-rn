import {ActivityIndicator} from 'react-native';
import React from 'react';

const LoadingIndicator: React.FC = () => (
  <ActivityIndicator size="large" color="#00CED0" />
);

export default LoadingIndicator;
